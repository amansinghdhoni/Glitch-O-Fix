Run this to fix all the code at once and to get hints in your terminal :- ``` curl ascii.live/rick ```

The script will try `mpv` (plays audio in terminal), then `xdg-open`/`open` to open your browser, and finally it will print the URL as a fallback.



